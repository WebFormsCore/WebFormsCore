name: .NET

on:
  push:
    branches:
      - main
      - ci/updated-publish

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v2

      - name: Setup .NET SDK
        uses: actions/setup-dotnet@v1
        with:
          dotnet-version: |
            6.0.x
            7.0.x
            8.0.100-preview.2.23157.25

      - name: Install dependencies
        run: dotnet restore

      - name: Build
        run: dotnet build --configuration Release --no-restore

      - name: Publish WebFormsCore
        uses: GerardSmit/publish-nuget@v3.1.1
        with:
          NUGET_KEY: ${{secrets.NUGET_API_KEY}}
          VERSION_FILE_PATH: Directory.Build.props
          PROJECT_FILE_PATH: |
            src/WebFormsCore/WebFormsCore.csproj
            src/WebFormsCore.AspNet/WebFormsCore.AspNet.csproj
            src/WebFormsCore.Owin/WebFormsCore.Owin.csproj
            src/WebFormsCore.AspNetCore/WebFormsCore.AspNetCore.csproj
            src/WebFormsCore.Parser/WebFormsCore.Parser.csproj
            src/WebFormsCore.Compiler/WebFormsCore.Compiler.csproj
            src/WebFormsCore.SourceGenerator/WebFormsCore.SourceGenerator.csproj
            sdk/WebFormsCore.SDK/WebFormsCore.SDK.csproj
            sdk/WebFormsCore.SDK.NetFramework/WebFormsCore.SDK.NetFramework.csproj
