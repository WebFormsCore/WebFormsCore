<Project>

  <PropertyGroup>
    <DebugType Condition=" '$(Configuration)' == 'Debug' ">Full</DebugType>
  </PropertyGroup>

  <Target Name="IncludeGeneratedSdkFiles" DependsOnTargets="ReplaceVersion" AfterTargets="CopyCommon" BeforeTargets="_GetPackageFiles">
    <ItemGroup>
      <None Include="Sdk\**" Pack="true" PackagePath="Sdk\" />
    </ItemGroup>
  </Target>

</Project>