<!--
    This code was generated by a tool.

    Changes to this file may cause incorrect behavior and will be lost if
    the code is regenerated.
-->

<Project>
    <Import Project="Core.targets" />

    <PropertyGroup>
        <UseOwin Condition="$(UseOwin) == ''">false</UseOwin>
    </PropertyGroup>

    <!-- NuGet references -->
    <ItemGroup Condition="$(WebFormsCoreUseNuget) == 'true'">
        <PackageReference Condition="$(UseOwin) != 'true'" Include="WebFormsCore.AspNet" Version="0.0.1-alpha.13" />
        <PackageReference Condition="$(UseOwin) == 'true'" Include="WebFormsCore.Owin" Version="0.0.1-alpha.13" />
    </ItemGroup>

    <ItemGroup>
        <PackageReference Include="HttpStack.AspNet" Version="0.0.1-alpha.1" />
    </ItemGroup>

    <PropertyGroup Condition="$(UseOwin) == 'true'">
        <DefineConstants>$(DefineConstants);WFC_OWIN</DefineConstants>
    </PropertyGroup>

    <PropertyGroup Condition="$(UseOwin) == 'false'">
        <DefineConstants>$(DefineConstants);WFC_ASPNET</DefineConstants>
    </PropertyGroup>

    <ItemGroup>
        <PackageReference Condition="$(UseOwin) == 'true'" Include="Microsoft.Owin.Host.SystemWeb" Version="4.2.2" />
    </ItemGroup>

    <Import Project="Sdk.targets" Sdk="MSBuild.SDK.SystemWeb" Version="4.0.88" />
</Project>
